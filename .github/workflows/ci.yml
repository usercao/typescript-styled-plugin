on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
name: Continuous Integration

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.20.0
          cache: yarn
      - run: corepack enable
      - run: yarn install --immutable
      - name: Formatting
        run: yarn format:check
      - name: Linting
        run: yarn lint
  compile:
    name: Compile and Typecheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.20.0
          cache: yarn
      - run: corepack enable
      - run: yarn install --immutable
      - name: Compiling
        run: yarn compile
      - name: Type checking
        run: yarn typecheck
      - name: Package API and runtime
        run: yarn test:package-api
      - name: Package contents
        run: npm pack --dry-run
  unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.20.0
          cache: yarn
      - run: corepack enable
      - run: yarn install --immutable
      - name: Compiling
        run: yarn compile
      - run: yarn test:unit
  e2e:
    name: E2E (${{ matrix.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Node 22.12 / TypeScript 6.0.2
            node-version: 22.12.0
            typescript-package: typescript-minimum
          - name: Node 22.12 / TypeScript current
            node-version: 22.12.0
            typescript-package: typescript-current
          - name: Node 24 / TypeScript current
            node-version: 24.20.0
            typescript-package: typescript-current
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: yarn
      - run: corepack enable
      - run: yarn install --immutable
      - name: Run tsserver scenarios
        run: yarn test:e2e
        env:
          TSSERVER_TYPESCRIPT_PACKAGE: ${{ matrix.typescript-package }}
